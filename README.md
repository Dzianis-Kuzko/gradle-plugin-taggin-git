# Gradle Git Tagging Plugin

#### 1 Add the plugin to your `build.gradle` 

```groovy
plugins {
    id 'ru.clevertec.plugin.taggingBrunch' version '1.0.0'
}
```

#### 2 Running the Plugin

```shell
./gradlew tagging
```
